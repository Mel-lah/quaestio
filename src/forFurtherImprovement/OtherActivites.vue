<script setup>
// import { ref, onMounted, computed, watch } from "vue";
// import QuestionList from "../pop-ups/QuestionList.vue";
// import TagList from "../pop-ups/TagList.vue";
// import { useCounterStore } from "../../stores/counter";
// import { storeToRefs } from "pinia";

// const { sQuestion, sTag } = storeToRefs(useCounterStore());
// const storeCounter = useCounterStore();
</script>

<template>
  <p>otheract</p>
  <!-- <div class="">
        <div class="flex flex-row items-center justify-evenly h-16 border-solid border-beigeD border-1 rounded shadow-md bg-beigeL">
            <button  @click="storeCounter.showQuestions" class=" bg-beigeD py-2 w-40 rounded-3xl justify-center inline-flex items-center font-semibold hover:bg-beigeSD">
                <img class="mr-2 w-3 h-3" src="/img/question-and-answer.png"/>
                Questions
            </button>
            <button @click="storeCounter.showTag" class="bg-beigeD py-2 w-40 rounded-3xl justify-center inline-flex items-center font-semibold hover:bg-beigeSD">
                <img class="mr-2 w-3 h-3" src="/img/tag.png"/>
                Tags 
            </button>
        </div>
    </div>


    <div v-if="sQuestion"
        class="fixed inset-0 z-50 bg-gray-500 bg-opacity-60   justify-center items-center">
        <QuestionList />
    </div>

    <div v-if="sTag"
        class="fixed inset-0 z-50 bg-gray-500 bg-opacity-60   justify-center items-center">
        <TagList />
    </div>
    
       
    <RouterView /> -->
</template>
